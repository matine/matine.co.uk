import { combineReducers } from 'redux';

export const prismicContent = (state = {}, action) => {
    return state;
};

const reducers = combineReducers({
    prismicContent,
});

export default reducers;
